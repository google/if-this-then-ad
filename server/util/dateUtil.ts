import {add, isAfter, isBefore, isValid, isFuture, isPast, parse, parseISO} from 'date-fns'

export const date = {
    add,
    isAfter, 
    isBefore, 
    isValid,
    isFuture,
    isPast, 
    parse, 
    parseISO
}