import { DynamicDataSource } from './dynamic-data-source.model';

describe('DynamicDataSource', () => {
  it('should create an instance', () => {
    expect(new DynamicDataSource()).toBeTruthy();
  });
});
