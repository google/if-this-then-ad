import { IntervalFormatPipe } from './interval-format.pipe';

describe('IntervalFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new IntervalFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
